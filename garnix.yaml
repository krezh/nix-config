---
# Config for garnix.io builds & caching
builds:
  exclude: ["nixosConfigurations.nixos-livecd"]
  include:
    - "*.x86_64-linux.*"
    - defaultPackage.x86_64-linux
    - devShell.x86_64-linux
    - homeConfigurations.*
    - nixosConfigurations.*
    - packages.x86_64-linux.*
